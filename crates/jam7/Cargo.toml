[package]
name = "jam7"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["2d", "ui"] }
bevy_enhanced_input.workspace = true

[features]
default = []
dev = ["bevy/dev"]
