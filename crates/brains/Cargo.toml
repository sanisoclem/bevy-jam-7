[package]
name = "brains"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy.workspace = true
burn = { version = "~0.20", features = [ "ndarray"] }
# burn = { version = "0.20.1", features = ["wgpu"] }
rand = "0.8"


[features]
default = []
dev = []
