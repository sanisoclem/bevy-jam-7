[package]
name = "sys_asset"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { workspace = true, features = ["2d", "ui"] }
serde.workspace = true

utils = { path = "../../crates/utils", version = "0.1.0" }
